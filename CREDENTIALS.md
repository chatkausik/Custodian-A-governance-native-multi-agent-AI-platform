# Browser-Facing Pages & Login Credentials

| #  | Page                                                                                         | URL                         | Login                                                                                                             | Where the Credential Comes From                                                                                                                                                                                                  |
| -- | -------------------------------------------------------------------------------------------- | --------------------------- | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | **Custodian Console** (the actual app — invoice pipeline, approvals, audit log, kill switch) | http://localhost:3000       | Pick one demo user: `ap.clerk.demo`, `controller.demo`, or `cfo.demo`                                             | Not its own login — it redirects to Keycloak (row 2) to authenticate                                                                                                                                                             |
| 2  | **Keycloak** (the login system itself — realm, roles, demo users)                            | http://localhost:8180/admin | Admin: `admin` / `e00c7d20c7db9b4d67682cda4a800654`                                                               | `.env` → `KEYCLOAK_ADMIN` / `KEYCLOAK_ADMIN_PASSWORD` — Keycloak's own container image reads these two env vars directly at first startup and creates that admin account itself                                                  |
| 2  | *Demo users (log in via Console, row 1, or here)*                                            |                             | `ap.clerk.demo` / `b7b875f9e135ebe1`<br>`controller.demo` / `b310507ffd40a935`<br>`cfo.demo` / `4b3ae1928486cf3d` | `.env` → `DEMO_AP_CLERK_PASSWORD` / `DEMO_CONTROLLER_PASSWORD` / `DEMO_CFO_PASSWORD` — these three specific users are created inside Keycloak's realm config file, which `render-keycloak-realm.sh` builds from `.env` in Step 0 |
| 3  | **Infisical** (the secrets vault)                                                            | http://localhost:8443       | `admin@custodian.local` / `a9abfe0aa6c0c18ced5016e8`                                                              | `.env` → `INFISICAL_ADMIN_EMAIL` / `INFISICAL_ADMIN_PASSWORD` — this account is genuinely created by calling Infisical's real signup API, done by `bootstrap-infisical.sh` in Step 2                                             |
| 4  | **MinIO console** (file storage — audit log bucket, etc.)                                    | http://localhost:9001       | `custodian_admin_f9dbb7bc` / `7cd9a9a5b08b210fe9969eb503a7d384`                                                   | `.env` → `MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD` — MinIO's own container reads these directly as its root account, no script involved                                                                                          |
| 5  | **LiteLLM proxy UI** (the AI gateway — usage, spend, keys)                                   | http://localhost:4000/ui    | Master key: `sk-custodian-master-f8ea4e4f923d2179c7efa17677ce49ca`                                                | `.env` → `LITELLM_MASTER_KEY` — LiteLLM's container reads this directly as its own admin key                                                                                                                                     |
| 6  | **MLflow** (prompt versions, pass/blocked gate results)                                      | http://localhost:5500       | None — open, no login configured                                                                                  | n/a (deliberately left unauthenticated for local-only use)                                                                                                                                                                       |
| 7  | **OpenMetadata** (data catalog — sensitivity tags on the invoice/vendor tables)              | http://localhost:8585       | `admin@open-metadata.org` / `admin`                                                                               | **Not from this project at all** — that's OpenMetadata's own out-of-the-box default admin account, unrelated to `.env`                                                                                                           |
| 8  | **Langfuse** (LLM call tracing/cost per agent)                                               | http://localhost:3010       | `admin@custodian.local` / `508ba64ca04098d266fa08a5`                                                              | `.env` → `LANGFUSE_INIT_USER_PASSWORD` — Langfuse's own container reads its `LANGFUSE_INIT_USER_*` env vars and creates that exact user on first startup                                                                         |
| 9  | **Prometheus** (raw metrics)                                                                 | http://localhost:9095       | None                                                                                                              | n/a                                                                                                                                                                                                                              |
| 10 | **Grafana** (dashboards built on Prometheus)                                                 | http://localhost:3020       | `admin` / `f0c22bfbdb6c1cdecee1efaf`                                                                              | `.env` → `GRAFANA_ADMIN_PASSWORD` — Grafana's own container reads this directly as its admin password                                                                                                                            |
